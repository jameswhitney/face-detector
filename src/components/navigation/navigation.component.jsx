import React from "react";

const Navigation = () => (
  <nav style={{ display: "flex", justifyContent: "flex-end" }}>
    <p className="f4 fa2 link dim black underline pa3 pointer">Sign Out</p>
  </nav>
);

export default Navigation;
